{
  "name": "@yummies/dom",
  "version": "1.0.0",
  "description": "ReactDOM ❤︎ BEM",
  "keywords": [ "yummies", "react", "bem", "bemjson" ],
  "homepage": "https://github.com/yummies/dom",
  "repository": "yummies/dom",
  "contributors": [
    "Kir Belevich <kir@soulshine.in> (https://github.com/deepsweet)",
    "Denis Koltsov <iam@mistadikay.com> (https://github.com/mistadikay)"
  ],
  "main": "build/index.js",
  "files": [
    "build/",
    "LICENSE"
  ],
  "dependencies": {
    "react-dom": "0.14.x",
    "@yummies/core": "1.0.x"
  },
  "devDependencies": {
    "babel-core": "6.3.x",
    "babel-cli": "6.3.x",
    "babel-preset-es2015": "6.3.x",
    "babel-preset-stage-0": "6.3.x",
    "babel-plugin-transform-proto-to-assign": "6.3.x",

    "react": "0.14.x",
    "husky": "0.10.x",
    "mocha": "2.3.x",
    "chai": "3.4.x",
    "isparta": "4.0.x",
    "coveralls": "2.11.x",
    "eslint": "1.10.x",
    "babel-eslint": ">5.0.0-beta1",
    "eslint-plugin-babel": "3.0.x",
    "jsdom": "7.2.x",
    "mocha-jsdom": "1.0.x",
    "chokidar-cli": "1.2.x"
  },
  "scripts": {
    "build": "rm -rf build && babel lib/ -d build/",
    "dev": "npm run build -- -w",
    "lint": "eslint lib test",

    "coverage": "rm -rf coverage/ && isparta cover _mocha -i lib/*.js -- -r babel/register test/setup.js test/lib/*.js",
    "tdd": "chokidar 'test/lib/*.js' -c 'npm run coverage' --initial",
    "test": "npm run lint && npm run coverage",
    "travis": "npm test && cat coverage/lcov.info | coveralls",

    "prepush": "npm test",
    "prepublish": "npm run build"
  },
  "engines": {
    "node": ">=4.0.0",
    "npm": ">=2.7.0"
  },
  "license": "MIT"
}
